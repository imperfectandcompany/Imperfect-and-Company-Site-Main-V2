// useNavigationData.ts
import { navigationData } from '../data/navigationData';

const useNavigationData = () => {
  // Directly return the imported data as it's static.
  return navigationData;
};

export default useNavigationData;